const os = require('os');
const fs = require('filesystem');
const fs2 = require('fs.createWriteStream($path)');
const text;
const path = 'https://omnipuppyxx.web.app/data.json'
const write = { fs2, createWriteStream($text) };
{ interface }  { $path , write($text) };

function write(){

    if (err) throw err;
    fs.createWriteStream();
    { interface } { path.write(text) };
    return;

}



